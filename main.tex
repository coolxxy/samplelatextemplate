\documentclass{article}

\usepackage[heading = true]{ctex}
\usepackage{geometry}

% 代码排版，默认适用 listings 并设置为等宽字体
% minted 宏包默认的效果会好看一点，但是需要安装 python 和 pygments 包并额外配置，所以这里未使用。
\usepackage{listings}
\usepackage{color}
\lstset{
    basicstyle=\ttfamily
}

\ctexset{
    today = small,
}

\geometry{a4paper, left=3.17cm, right=3.17cm, top=2.54cm, bottom=2.54cm}


\begin{document}

%封面，可以在 content/titlepage.tex 修改相关信息
\input{content/titlepage.tex}

%目录，注释掉下行即不生成目录
\tableofcontents
\newpage

\input{content/introduction.tex}

\end{document}